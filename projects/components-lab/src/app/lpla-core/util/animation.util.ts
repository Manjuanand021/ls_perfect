export const CSS_TRANSITION_LENGTH: number = 500; //ms
export const SCROLL_SPEED: number = 500; //ms

export function scrollTo(position: number): void {
    // $("html, body").animate({
    // 	scrollTop: position
    // }, SCROLL_SPEED);
}
